# Slim API/static image deps for DEEPFAKE_MODE=modal. No torch/timm/opencv/cuda -
# real inference happens on Modal; this process only dispatches and polls.

# FastAPI stack
fastapi
uvicorn[standard]
python-multipart

# Auth & security
python-jose[cryptography]
google-auth

# Database
sqlalchemy

# Env
python-dotenv

# Payments
razorpay

# Modal client
modal

# Pull model metadata (config.json) from Hugging Face so the catalog builds in modal mode
huggingface_hub

# Optional but recommended
requests
